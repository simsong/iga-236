codecov:
  require_ci_to_pass: yes

coverage:
  precision: 2
  round: down
  range: "70...100"
  
  ignore:
    - "src/**/*.test.ts"
    - "node_modules/**"
    - "build/**"
    - "coverage/**"

comment:
  layout: "reach, diff, flags, files, footer"
  behavior: default
  require_changes: no

